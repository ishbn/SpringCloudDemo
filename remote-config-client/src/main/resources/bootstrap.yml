spring:
  cloud:
    config:
      name: remoteclient
      label: master
      discovery:
        enabled: true
        service-id: configserver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8200/eureka/



